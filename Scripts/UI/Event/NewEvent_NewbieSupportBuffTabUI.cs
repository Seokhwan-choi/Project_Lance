using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Mosframe;
using System.Linq;

namespace Lance
{
    class NewEvent_NewbieSupportBuffTabUI : NewEventTabUI
    {
        public override void Init(NewEventTabUIManager parent, NewEventTab tab)
        {
            base.Init(parent, tab);
        }
    }
}